build/
*.o
*.elf
*.img
*.bin
fuzz/corpus/
fuzz/findings/
.venv/
__pycache__/
.coverage
.pytest_cache/
.mutmut-cache
.claude/*
# Whitelist committed safe-defaults; everything else (settings.local.json,
# agents/, hooks/, etc.) stays ignored as before.
!.claude/settings.json
